// import validate from '../../validator/index.js';
// import * as mainUtils from '../../utils.js';
// import * as parserUtils from '../../parser/index.js';
// import * as u from '../utils.js';

export default function handleFunction(scalarType, ast, parameter, generics) {
    return false;

    // const targetAst = parserUtils.splitTypeDeclaration(parameter[mainUtils.typeDeclaration]);
    // // console.log(targetAst);
    // // console.log(ast, generics);

    // console.log(parameter[mainUtils.typeDeclaration])

    // const initial = { generics };
    // return u.yieldResult(
    //     parameter.reduce(
    //         (accum, parameter) => validate(ast, scalarType.description, [].concat(parameter), accum.generics),
    //         initial
    //     )
    // );
}
